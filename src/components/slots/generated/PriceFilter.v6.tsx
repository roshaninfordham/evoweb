// Generated by the EvoWeb evolution engine — slot "price-filter", version 6.
// Price filter
// Several visitors searched for sneakers under $100 or under $50, and one left feedback asking for sneakers under $50, so a price filter would directly address what they were looking for.
//
// This file is an audit-trail archive of what was verified and deployed at runtime;
// it is not imported by the running app (slots render from the database at runtime).

function Component(props: PriceFilterProps) {
  return (
    <SlotPanel>
      <SlotLabel>Under ${props.value}</SlotLabel>
      <SlotRangeSlider
        min={props.min}
        max={props.max}
        value={props.value}
        onChange={props.onChange}
      />
    </SlotPanel>
  );
}

export default Component;
