// Generated by the EvoWeb evolution engine — slot "compare-products", version 1.
// Compare products
// Planner timed out; building based on observed evidence: Visitor opened detail views on 2 different products in this visit: Drift Runner, Night Trail.
//
// This file is an audit-trail archive of what was verified and deployed at runtime;
// it is not imported by the running app (slots render from the database at runtime).

function Component(props: CompareProductsProps) {
  const { products } = props;

  return (
    <SlotPanel>
      <SlotLabel>
        Comparing {products.map((p) => p.name).join(" vs ")}
      </SlotLabel>
      <SlotTable
        headers={["", ...products.map((p) => p.name)]}
        rows={[
          {
            label: "Price",
            values: products.map((p) => `$${p.price.toFixed(2)}`),
          },
          {
            label: "Weight",
            values: products.map((p) => `${p.weightGrams}g`),
          },
          {
            label: "Rating",
            values: products.map((p) => `${p.rating.toFixed(1)} / 5`),
          },
        ]}
      />
    </SlotPanel>
  );
}

export default Component;
