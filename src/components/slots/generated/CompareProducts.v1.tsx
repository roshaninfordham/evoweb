// Generated by the EvoWeb evolution engine — slot "compare-products", version 1.
// Compare products
// A visitor opened detail views for two different products (Drift Runner and Field Low) in one visit, indicating they were deciding between them. A compare products tool is a standard storefront feature that supports this kind of decision-making.
//
// This file is an audit-trail archive of what was verified and deployed at runtime;
// it is not imported by the running app (slots render from the database at runtime).

function Component(props: CompareProductsProps) {
  const columns = props.products.map((p) => p.name);
  const rows = [
    { label: "Price", values: props.products.map((p) => "$" + p.price) },
    { label: "Weight", values: props.products.map((p) => p.weightGrams + "g") },
    { label: "Rating", values: props.products.map((p) => String(p.rating)) },
  ];
  return (
    <SlotPanel>
      <SlotLabel>{"Comparing " + props.products.length + " products"}</SlotLabel>
      <SlotTable columns={columns} rows={rows} />
    </SlotPanel>
  );
}

export default Component;
